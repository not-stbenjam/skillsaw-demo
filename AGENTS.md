# Agent Guidelines

When working with agents in this repository, follow these guidelines.

## Working with Files

When editing files in the codebase, check the folder structure before
creating new directories. Run `npm run build` after changes to
`src/` and verify output in `dist/`.

## Pull Requests

Create a PR for each logical change. Run `npm test` before opening.
Include a description of what changed and why.
